# 智鸽·中枢管家模块功能说明

> 完整的管家功能模块说明和使用指南

## 📦 模块架构

### 核心模块（Core）
**位置**：`src/core/`

#### 1. `admin-agent.js` - 管理员代理
- 系统健康检查
- API监控协调
- 数据验证协调
- 定时任务管理
- 智能功能集成

#### 2. `core-admin-manager.js` - 核心管理器 ✨ 新增
- **统一管理所有模块**
- 模块初始化和验证
- 系统状态汇总
- 功能能力展示
- 优雅关闭管理

### 基础模块（Base）
**位置**：`src/`

#### 1. `api-sentinel/` - API监管
- 自动发现API
- 实时健康监测
- 数据真实性校验

#### 2. `truth-validator/` - 数据真实性验证
- 多源交叉验证
- 数据标记（verified/suspect/invalid）

#### 3. `rule-engine/` - 规则引擎
- 阈值规则检查
- 业务逻辑验证
- 风控规则执行

#### 4. `ai-hub/` - AI调度中枢
- 自动判断是否调用AI
- 控制调用频率和成本
- 统一封装AI接口

#### 5. `learning/` - 学习引擎
- 行为学习
- 结果学习
- 记忆系统

#### 6. `analytics/` - 数据分析器
- 用户行为分析
- 点击率分析
- 数据上传模式分析

#### 7. `maintenance/` - 自动维护器
- Bug检测
- 自动修复
- 代码升级

### 增强模块（Enhanced）
**位置**：`src/enhanced/`

#### 1. `user-manager.js` - 用户管理增强
- 异常用户检测
- 行为分析
- 自动处理

#### 2. `data-manager.js` - 数据管理增强
- 数据质量检查
- 重复数据检测
- 自动清理
- 智能备份

#### 3. `performance-optimizer.js` - 性能优化器
- 性能监控
- 瓶颈分析
- 自动优化

#### 4. `security-monitor.js` - 安全监控
- 威胁检测
- 速率限制
- IP阻止
- 输入验证

#### 5. `report-generator.js` - 报告生成器
- 综合报告生成
- 趋势分析

### 智能模块（Intelligent）✨ 新增
**位置**：`src/intelligent/`

#### 1. `decision-engine.js` - 智能决策引擎
**功能**：
- 基于规则和AI的自动决策
- 支持多类别决策
- 自动执行低风险决策
- 决策历史学习

**决策类别**：
- `systemHealth` - 系统健康
- `apiHealth` - API健康
- `dataQuality` - 数据质量
- `userBehavior` - 用户行为

#### 2. `predictive-maintenance.js` - 预测性维护
**功能**：
- 预测API故障
- 预测数据质量问题
- 预测系统性能问题
- 预测用户行为异常

**预测模型**：
- API故障预测模型
- 数据质量预测模型
- 系统性能预测模型
- 用户行为预测模型

#### 3. `workflow-engine.js` - 自动化工作流引擎
**功能**：
- 自动化常见管理任务
- 支持条件执行
- 支持重试机制
- 工作流模板系统

**内置工作流**：
- `dailyHealthCheck` - 每日健康检查
- `dataCleanup` - 数据清理
- `performanceOptimization` - 性能优化
- `securityScan` - 安全扫描
- `intelligentBackup` - 智能备份

#### 4. `alert-system.js` - 智能告警系统
**功能**：
- 智能分析和分级告警
- 告警冷却机制
- AI增强分析
- 多渠道通知支持

**告警级别**：
- `critical` - 关键（立即通知，自动干预）
- `high` - 高（通知，增强监控）
- `medium` - 中（记录，安排检查）
- `low` - 低（仅记录）

#### 5. `adaptive-optimizer.js` - 自适应优化模块
**功能**：
- 根据系统状态自动调整配置
- 性能基线跟踪
- 优化效果验证
- 配置自动合并

**优化领域**：
- 缓存TTL调整
- API重试策略
- 并发控制
- 资源使用优化

#### 6. `knowledge-graph.js` - 知识图谱系统
**功能**：
- 构建系统知识库
- 实体关系管理
- 智能查询
- 问题解决方案记录

**关系类型**：
- `depends_on` - 依赖
- `causes` - 导致
- `affects` - 影响
- `related_to` - 相关
- `similar_to` - 相似
- `solves` - 解决

#### 7. `report-generator.js` - 智能报告生成器
**功能**：
- AI生成管理报告
- 多类型报告（每日/每周/每月）
- 多章节结构化报告
- AI摘要生成

**报告章节**：
- 摘要
- 系统健康
- 性能指标
- 问题列表
- 趋势分析
- 用户统计
- 数据质量
- 优化建议
- AI管理建议

## 🎯 核心管理器功能

### CoreAdminManager 类

**主要方法**：

1. **`initialize()`** - 初始化所有模块
2. **`getSystemStatus()`** - 获取系统状态
3. **`performHealthCheck()`** - 执行健康检查
4. **`getIntelligentAdvice()`** - 获取智能建议
5. **`executeWorkflow()`** - 执行工作流
6. **`queryKnowledge()`** - 查询知识图谱
7. **`generateReport()`** - 生成智能报告
8. **`getAlerts()`** - 获取告警
9. **`getWorkflows()`** - 获取工作流
10. **`getPredictions()`** - 获取预测信息
11. **`getOptimizationStatus()`** - 获取优化状态
12. **`makeDecision()`** - 执行智能决策
13. **`createAlert()`** - 创建告警
14. **`getAllCapabilities()`** - 获取所有功能能力

## 📡 API端点

### 系统状态
- `GET /api/status` - 获取完整系统状态（包含所有模块）
- `GET /api/modules/:type` - 获取特定模块信息（core/base/enhanced/intelligent）
- `GET /api/capabilities` - 获取所有功能能力

### 智能功能
- `GET /api/intelligent/advice` - 智能决策建议
- `GET /api/intelligent/predictions` - 预测性维护
- `GET /api/intelligent/workflows` - 工作流管理
- `GET /api/intelligent/alerts` - 告警管理
- `GET /api/intelligent/optimization` - 优化状态
- `GET /api/intelligent/knowledge` - 知识图谱查询
- `GET /api/intelligent/reports` - 智能报告

## 🔧 使用示例

### 1. 获取系统状态

```javascript
const coreAdminManager = require('./core/core-admin-manager');

// 获取完整系统状态
const status = coreAdminManager.getSystemStatus();
console.log(status);
```

### 2. 执行智能决策

```javascript
// 执行系统健康决策
const decision = await coreAdminManager.makeDecision('systemHealth', {
  health: 0.75,
  issues: []
});
```

### 3. 执行工作流

```javascript
// 执行每日健康检查工作流
const result = await coreAdminManager.executeWorkflow('dailyHealthCheck', {});
```

### 4. 查询知识图谱

```javascript
// 查询API相关问题
const result = await coreAdminManager.queryKnowledge('API故障');
```

### 5. 生成智能报告

```javascript
// 生成每日报告
const report = await coreAdminManager.generateReport('daily');
```

## 📊 模块统计

### 模块数量
- **核心模块**：2个
- **基础模块**：7个
- **增强模块**：5个
- **智能模块**：7个
- **总计**：21个功能模块

### 功能覆盖
- ✅ 系统监控
- ✅ API监管
- ✅ 数据验证
- ✅ 用户管理
- ✅ 数据管理
- ✅ 性能优化
- ✅ 安全监控
- ✅ 智能决策
- ✅ 预测性维护
- ✅ 自动化工作流
- ✅ 智能告警
- ✅ 自适应优化
- ✅ 知识图谱
- ✅ 智能报告

## 🚀 启动流程

1. **加载配置**：读取环境变量和配置文件
2. **初始化核心管理器**：`coreAdminManager.initialize()`
3. **初始化核心模块**：`adminAgent.initialize()`
4. **验证基础模块**：检查所有基础模块是否正常
5. **验证增强模块**：检查所有增强模块是否正常
6. **验证智能模块**：检查所有智能模块是否正常
7. **注册默认工作流**：注册5个内置工作流
8. **启动定时任务**：启动所有定时任务
9. **启动HTTP服务器**：监听端口，提供API服务

## 📝 模块依赖关系

```
core-admin-manager (核心管理器)
├── admin-agent (管理员代理)
│   ├── api-sentinel (API监管)
│   ├── truth-validator (数据验证)
│   ├── rule-engine (规则引擎)
│   ├── ai-hub (AI中枢)
│   ├── learning-engine (学习引擎)
│   ├── data-analyzer (数据分析)
│   └── auto-maintainer (自动维护)
├── enhanced (增强模块)
│   ├── user-manager
│   ├── data-manager
│   ├── performance-optimizer
│   ├── security-monitor
│   └── report-generator
└── intelligent (智能模块)
    ├── decision-engine
    ├── predictive-maintenance
    ├── workflow-engine
    ├── alert-system
    ├── adaptive-optimizer
    ├── knowledge-graph
    └── report-generator
```

## 🎨 后台界面集成

所有功能已集成到后台管理系统：

1. **系统状态页面**：显示所有模块状态
2. **智能建议页面**：显示AI决策建议
3. **工作流管理页面**：管理工作流
4. **告警中心页面**：查看和管理告警
5. **知识图谱页面**：查询系统知识
6. **智能报告页面**：查看和生成报告
7. **模块管理页面**：查看各模块信息

## ⚙️ 配置说明

### 环境变量

```env
# 中枢管家专用API Key
ZHIPU_API_KEY_ADMIN=your_api_key
ZHIPU_MODEL=glm-4

# 服务器配置
PORT=3001
NODE_ENV=production
```

### 模块配置

各模块的配置可以在对应的配置文件中修改：
- `src/config/config.js` - 主配置文件
- 各模块内部配置

## 🔍 故障排查

### 模块未加载

1. 检查日志：`logs/combined.log`
2. 检查模块文件是否存在
3. 检查模块依赖是否正确

### 功能不工作

1. 检查模块是否已初始化
2. 检查API Key配置
3. 查看错误日志

---

**版本**：v3.0.0（智能增强版）
**最后更新**：2025-01-XX







> 完整的管家功能模块说明和使用指南

## 📦 模块架构

### 核心模块（Core）
**位置**：`src/core/`

#### 1. `admin-agent.js` - 管理员代理
- 系统健康检查
- API监控协调
- 数据验证协调
- 定时任务管理
- 智能功能集成

#### 2. `core-admin-manager.js` - 核心管理器 ✨ 新增
- **统一管理所有模块**
- 模块初始化和验证
- 系统状态汇总
- 功能能力展示
- 优雅关闭管理

### 基础模块（Base）
**位置**：`src/`

#### 1. `api-sentinel/` - API监管
- 自动发现API
- 实时健康监测
- 数据真实性校验

#### 2. `truth-validator/` - 数据真实性验证
- 多源交叉验证
- 数据标记（verified/suspect/invalid）

#### 3. `rule-engine/` - 规则引擎
- 阈值规则检查
- 业务逻辑验证
- 风控规则执行

#### 4. `ai-hub/` - AI调度中枢
- 自动判断是否调用AI
- 控制调用频率和成本
- 统一封装AI接口

#### 5. `learning/` - 学习引擎
- 行为学习
- 结果学习
- 记忆系统

#### 6. `analytics/` - 数据分析器
- 用户行为分析
- 点击率分析
- 数据上传模式分析

#### 7. `maintenance/` - 自动维护器
- Bug检测
- 自动修复
- 代码升级

### 增强模块（Enhanced）
**位置**：`src/enhanced/`

#### 1. `user-manager.js` - 用户管理增强
- 异常用户检测
- 行为分析
- 自动处理

#### 2. `data-manager.js` - 数据管理增强
- 数据质量检查
- 重复数据检测
- 自动清理
- 智能备份

#### 3. `performance-optimizer.js` - 性能优化器
- 性能监控
- 瓶颈分析
- 自动优化

#### 4. `security-monitor.js` - 安全监控
- 威胁检测
- 速率限制
- IP阻止
- 输入验证

#### 5. `report-generator.js` - 报告生成器
- 综合报告生成
- 趋势分析

### 智能模块（Intelligent）✨ 新增
**位置**：`src/intelligent/`

#### 1. `decision-engine.js` - 智能决策引擎
**功能**：
- 基于规则和AI的自动决策
- 支持多类别决策
- 自动执行低风险决策
- 决策历史学习

**决策类别**：
- `systemHealth` - 系统健康
- `apiHealth` - API健康
- `dataQuality` - 数据质量
- `userBehavior` - 用户行为

#### 2. `predictive-maintenance.js` - 预测性维护
**功能**：
- 预测API故障
- 预测数据质量问题
- 预测系统性能问题
- 预测用户行为异常

**预测模型**：
- API故障预测模型
- 数据质量预测模型
- 系统性能预测模型
- 用户行为预测模型

#### 3. `workflow-engine.js` - 自动化工作流引擎
**功能**：
- 自动化常见管理任务
- 支持条件执行
- 支持重试机制
- 工作流模板系统

**内置工作流**：
- `dailyHealthCheck` - 每日健康检查
- `dataCleanup` - 数据清理
- `performanceOptimization` - 性能优化
- `securityScan` - 安全扫描
- `intelligentBackup` - 智能备份

#### 4. `alert-system.js` - 智能告警系统
**功能**：
- 智能分析和分级告警
- 告警冷却机制
- AI增强分析
- 多渠道通知支持

**告警级别**：
- `critical` - 关键（立即通知，自动干预）
- `high` - 高（通知，增强监控）
- `medium` - 中（记录，安排检查）
- `low` - 低（仅记录）

#### 5. `adaptive-optimizer.js` - 自适应优化模块
**功能**：
- 根据系统状态自动调整配置
- 性能基线跟踪
- 优化效果验证
- 配置自动合并

**优化领域**：
- 缓存TTL调整
- API重试策略
- 并发控制
- 资源使用优化

#### 6. `knowledge-graph.js` - 知识图谱系统
**功能**：
- 构建系统知识库
- 实体关系管理
- 智能查询
- 问题解决方案记录

**关系类型**：
- `depends_on` - 依赖
- `causes` - 导致
- `affects` - 影响
- `related_to` - 相关
- `similar_to` - 相似
- `solves` - 解决

#### 7. `report-generator.js` - 智能报告生成器
**功能**：
- AI生成管理报告
- 多类型报告（每日/每周/每月）
- 多章节结构化报告
- AI摘要生成

**报告章节**：
- 摘要
- 系统健康
- 性能指标
- 问题列表
- 趋势分析
- 用户统计
- 数据质量
- 优化建议
- AI管理建议

## 🎯 核心管理器功能

### CoreAdminManager 类

**主要方法**：

1. **`initialize()`** - 初始化所有模块
2. **`getSystemStatus()`** - 获取系统状态
3. **`performHealthCheck()`** - 执行健康检查
4. **`getIntelligentAdvice()`** - 获取智能建议
5. **`executeWorkflow()`** - 执行工作流
6. **`queryKnowledge()`** - 查询知识图谱
7. **`generateReport()`** - 生成智能报告
8. **`getAlerts()`** - 获取告警
9. **`getWorkflows()`** - 获取工作流
10. **`getPredictions()`** - 获取预测信息
11. **`getOptimizationStatus()`** - 获取优化状态
12. **`makeDecision()`** - 执行智能决策
13. **`createAlert()`** - 创建告警
14. **`getAllCapabilities()`** - 获取所有功能能力

## 📡 API端点

### 系统状态
- `GET /api/status` - 获取完整系统状态（包含所有模块）
- `GET /api/modules/:type` - 获取特定模块信息（core/base/enhanced/intelligent）
- `GET /api/capabilities` - 获取所有功能能力

### 智能功能
- `GET /api/intelligent/advice` - 智能决策建议
- `GET /api/intelligent/predictions` - 预测性维护
- `GET /api/intelligent/workflows` - 工作流管理
- `GET /api/intelligent/alerts` - 告警管理
- `GET /api/intelligent/optimization` - 优化状态
- `GET /api/intelligent/knowledge` - 知识图谱查询
- `GET /api/intelligent/reports` - 智能报告

## 🔧 使用示例

### 1. 获取系统状态

```javascript
const coreAdminManager = require('./core/core-admin-manager');

// 获取完整系统状态
const status = coreAdminManager.getSystemStatus();
console.log(status);
```

### 2. 执行智能决策

```javascript
// 执行系统健康决策
const decision = await coreAdminManager.makeDecision('systemHealth', {
  health: 0.75,
  issues: []
});
```

### 3. 执行工作流

```javascript
// 执行每日健康检查工作流
const result = await coreAdminManager.executeWorkflow('dailyHealthCheck', {});
```

### 4. 查询知识图谱

```javascript
// 查询API相关问题
const result = await coreAdminManager.queryKnowledge('API故障');
```

### 5. 生成智能报告

```javascript
// 生成每日报告
const report = await coreAdminManager.generateReport('daily');
```

## 📊 模块统计

### 模块数量
- **核心模块**：2个
- **基础模块**：7个
- **增强模块**：5个
- **智能模块**：7个
- **总计**：21个功能模块

### 功能覆盖
- ✅ 系统监控
- ✅ API监管
- ✅ 数据验证
- ✅ 用户管理
- ✅ 数据管理
- ✅ 性能优化
- ✅ 安全监控
- ✅ 智能决策
- ✅ 预测性维护
- ✅ 自动化工作流
- ✅ 智能告警
- ✅ 自适应优化
- ✅ 知识图谱
- ✅ 智能报告

## 🚀 启动流程

1. **加载配置**：读取环境变量和配置文件
2. **初始化核心管理器**：`coreAdminManager.initialize()`
3. **初始化核心模块**：`adminAgent.initialize()`
4. **验证基础模块**：检查所有基础模块是否正常
5. **验证增强模块**：检查所有增强模块是否正常
6. **验证智能模块**：检查所有智能模块是否正常
7. **注册默认工作流**：注册5个内置工作流
8. **启动定时任务**：启动所有定时任务
9. **启动HTTP服务器**：监听端口，提供API服务

## 📝 模块依赖关系

```
core-admin-manager (核心管理器)
├── admin-agent (管理员代理)
│   ├── api-sentinel (API监管)
│   ├── truth-validator (数据验证)
│   ├── rule-engine (规则引擎)
│   ├── ai-hub (AI中枢)
│   ├── learning-engine (学习引擎)
│   ├── data-analyzer (数据分析)
│   └── auto-maintainer (自动维护)
├── enhanced (增强模块)
│   ├── user-manager
│   ├── data-manager
│   ├── performance-optimizer
│   ├── security-monitor
│   └── report-generator
└── intelligent (智能模块)
    ├── decision-engine
    ├── predictive-maintenance
    ├── workflow-engine
    ├── alert-system
    ├── adaptive-optimizer
    ├── knowledge-graph
    └── report-generator
```

## 🎨 后台界面集成

所有功能已集成到后台管理系统：

1. **系统状态页面**：显示所有模块状态
2. **智能建议页面**：显示AI决策建议
3. **工作流管理页面**：管理工作流
4. **告警中心页面**：查看和管理告警
5. **知识图谱页面**：查询系统知识
6. **智能报告页面**：查看和生成报告
7. **模块管理页面**：查看各模块信息

## ⚙️ 配置说明

### 环境变量

```env
# 中枢管家专用API Key
ZHIPU_API_KEY_ADMIN=your_api_key
ZHIPU_MODEL=glm-4

# 服务器配置
PORT=3001
NODE_ENV=production
```

### 模块配置

各模块的配置可以在对应的配置文件中修改：
- `src/config/config.js` - 主配置文件
- 各模块内部配置

## 🔍 故障排查

### 模块未加载

1. 检查日志：`logs/combined.log`
2. 检查模块文件是否存在
3. 检查模块依赖是否正确

### 功能不工作

1. 检查模块是否已初始化
2. 检查API Key配置
3. 查看错误日志

---

**版本**：v3.0.0（智能增强版）
**最后更新**：2025-01-XX







> 完整的管家功能模块说明和使用指南

## 📦 模块架构

### 核心模块（Core）
**位置**：`src/core/`

#### 1. `admin-agent.js` - 管理员代理
- 系统健康检查
- API监控协调
- 数据验证协调
- 定时任务管理
- 智能功能集成

#### 2. `core-admin-manager.js` - 核心管理器 ✨ 新增
- **统一管理所有模块**
- 模块初始化和验证
- 系统状态汇总
- 功能能力展示
- 优雅关闭管理

### 基础模块（Base）
**位置**：`src/`

#### 1. `api-sentinel/` - API监管
- 自动发现API
- 实时健康监测
- 数据真实性校验

#### 2. `truth-validator/` - 数据真实性验证
- 多源交叉验证
- 数据标记（verified/suspect/invalid）

#### 3. `rule-engine/` - 规则引擎
- 阈值规则检查
- 业务逻辑验证
- 风控规则执行

#### 4. `ai-hub/` - AI调度中枢
- 自动判断是否调用AI
- 控制调用频率和成本
- 统一封装AI接口

#### 5. `learning/` - 学习引擎
- 行为学习
- 结果学习
- 记忆系统

#### 6. `analytics/` - 数据分析器
- 用户行为分析
- 点击率分析
- 数据上传模式分析

#### 7. `maintenance/` - 自动维护器
- Bug检测
- 自动修复
- 代码升级

### 增强模块（Enhanced）
**位置**：`src/enhanced/`

#### 1. `user-manager.js` - 用户管理增强
- 异常用户检测
- 行为分析
- 自动处理

#### 2. `data-manager.js` - 数据管理增强
- 数据质量检查
- 重复数据检测
- 自动清理
- 智能备份

#### 3. `performance-optimizer.js` - 性能优化器
- 性能监控
- 瓶颈分析
- 自动优化

#### 4. `security-monitor.js` - 安全监控
- 威胁检测
- 速率限制
- IP阻止
- 输入验证

#### 5. `report-generator.js` - 报告生成器
- 综合报告生成
- 趋势分析

### 智能模块（Intelligent）✨ 新增
**位置**：`src/intelligent/`

#### 1. `decision-engine.js` - 智能决策引擎
**功能**：
- 基于规则和AI的自动决策
- 支持多类别决策
- 自动执行低风险决策
- 决策历史学习

**决策类别**：
- `systemHealth` - 系统健康
- `apiHealth` - API健康
- `dataQuality` - 数据质量
- `userBehavior` - 用户行为

#### 2. `predictive-maintenance.js` - 预测性维护
**功能**：
- 预测API故障
- 预测数据质量问题
- 预测系统性能问题
- 预测用户行为异常

**预测模型**：
- API故障预测模型
- 数据质量预测模型
- 系统性能预测模型
- 用户行为预测模型

#### 3. `workflow-engine.js` - 自动化工作流引擎
**功能**：
- 自动化常见管理任务
- 支持条件执行
- 支持重试机制
- 工作流模板系统

**内置工作流**：
- `dailyHealthCheck` - 每日健康检查
- `dataCleanup` - 数据清理
- `performanceOptimization` - 性能优化
- `securityScan` - 安全扫描
- `intelligentBackup` - 智能备份

#### 4. `alert-system.js` - 智能告警系统
**功能**：
- 智能分析和分级告警
- 告警冷却机制
- AI增强分析
- 多渠道通知支持

**告警级别**：
- `critical` - 关键（立即通知，自动干预）
- `high` - 高（通知，增强监控）
- `medium` - 中（记录，安排检查）
- `low` - 低（仅记录）

#### 5. `adaptive-optimizer.js` - 自适应优化模块
**功能**：
- 根据系统状态自动调整配置
- 性能基线跟踪
- 优化效果验证
- 配置自动合并

**优化领域**：
- 缓存TTL调整
- API重试策略
- 并发控制
- 资源使用优化

#### 6. `knowledge-graph.js` - 知识图谱系统
**功能**：
- 构建系统知识库
- 实体关系管理
- 智能查询
- 问题解决方案记录

**关系类型**：
- `depends_on` - 依赖
- `causes` - 导致
- `affects` - 影响
- `related_to` - 相关
- `similar_to` - 相似
- `solves` - 解决

#### 7. `report-generator.js` - 智能报告生成器
**功能**：
- AI生成管理报告
- 多类型报告（每日/每周/每月）
- 多章节结构化报告
- AI摘要生成

**报告章节**：
- 摘要
- 系统健康
- 性能指标
- 问题列表
- 趋势分析
- 用户统计
- 数据质量
- 优化建议
- AI管理建议

## 🎯 核心管理器功能

### CoreAdminManager 类

**主要方法**：

1. **`initialize()`** - 初始化所有模块
2. **`getSystemStatus()`** - 获取系统状态
3. **`performHealthCheck()`** - 执行健康检查
4. **`getIntelligentAdvice()`** - 获取智能建议
5. **`executeWorkflow()`** - 执行工作流
6. **`queryKnowledge()`** - 查询知识图谱
7. **`generateReport()`** - 生成智能报告
8. **`getAlerts()`** - 获取告警
9. **`getWorkflows()`** - 获取工作流
10. **`getPredictions()`** - 获取预测信息
11. **`getOptimizationStatus()`** - 获取优化状态
12. **`makeDecision()`** - 执行智能决策
13. **`createAlert()`** - 创建告警
14. **`getAllCapabilities()`** - 获取所有功能能力

## 📡 API端点

### 系统状态
- `GET /api/status` - 获取完整系统状态（包含所有模块）
- `GET /api/modules/:type` - 获取特定模块信息（core/base/enhanced/intelligent）
- `GET /api/capabilities` - 获取所有功能能力

### 智能功能
- `GET /api/intelligent/advice` - 智能决策建议
- `GET /api/intelligent/predictions` - 预测性维护
- `GET /api/intelligent/workflows` - 工作流管理
- `GET /api/intelligent/alerts` - 告警管理
- `GET /api/intelligent/optimization` - 优化状态
- `GET /api/intelligent/knowledge` - 知识图谱查询
- `GET /api/intelligent/reports` - 智能报告

## 🔧 使用示例

### 1. 获取系统状态

```javascript
const coreAdminManager = require('./core/core-admin-manager');

// 获取完整系统状态
const status = coreAdminManager.getSystemStatus();
console.log(status);
```

### 2. 执行智能决策

```javascript
// 执行系统健康决策
const decision = await coreAdminManager.makeDecision('systemHealth', {
  health: 0.75,
  issues: []
});
```

### 3. 执行工作流

```javascript
// 执行每日健康检查工作流
const result = await coreAdminManager.executeWorkflow('dailyHealthCheck', {});
```

### 4. 查询知识图谱

```javascript
// 查询API相关问题
const result = await coreAdminManager.queryKnowledge('API故障');
```

### 5. 生成智能报告

```javascript
// 生成每日报告
const report = await coreAdminManager.generateReport('daily');
```

## 📊 模块统计

### 模块数量
- **核心模块**：2个
- **基础模块**：7个
- **增强模块**：5个
- **智能模块**：7个
- **总计**：21个功能模块

### 功能覆盖
- ✅ 系统监控
- ✅ API监管
- ✅ 数据验证
- ✅ 用户管理
- ✅ 数据管理
- ✅ 性能优化
- ✅ 安全监控
- ✅ 智能决策
- ✅ 预测性维护
- ✅ 自动化工作流
- ✅ 智能告警
- ✅ 自适应优化
- ✅ 知识图谱
- ✅ 智能报告

## 🚀 启动流程

1. **加载配置**：读取环境变量和配置文件
2. **初始化核心管理器**：`coreAdminManager.initialize()`
3. **初始化核心模块**：`adminAgent.initialize()`
4. **验证基础模块**：检查所有基础模块是否正常
5. **验证增强模块**：检查所有增强模块是否正常
6. **验证智能模块**：检查所有智能模块是否正常
7. **注册默认工作流**：注册5个内置工作流
8. **启动定时任务**：启动所有定时任务
9. **启动HTTP服务器**：监听端口，提供API服务

## 📝 模块依赖关系

```
core-admin-manager (核心管理器)
├── admin-agent (管理员代理)
│   ├── api-sentinel (API监管)
│   ├── truth-validator (数据验证)
│   ├── rule-engine (规则引擎)
│   ├── ai-hub (AI中枢)
│   ├── learning-engine (学习引擎)
│   ├── data-analyzer (数据分析)
│   └── auto-maintainer (自动维护)
├── enhanced (增强模块)
│   ├── user-manager
│   ├── data-manager
│   ├── performance-optimizer
│   ├── security-monitor
│   └── report-generator
└── intelligent (智能模块)
    ├── decision-engine
    ├── predictive-maintenance
    ├── workflow-engine
    ├── alert-system
    ├── adaptive-optimizer
    ├── knowledge-graph
    └── report-generator
```

## 🎨 后台界面集成

所有功能已集成到后台管理系统：

1. **系统状态页面**：显示所有模块状态
2. **智能建议页面**：显示AI决策建议
3. **工作流管理页面**：管理工作流
4. **告警中心页面**：查看和管理告警
5. **知识图谱页面**：查询系统知识
6. **智能报告页面**：查看和生成报告
7. **模块管理页面**：查看各模块信息

## ⚙️ 配置说明

### 环境变量

```env
# 中枢管家专用API Key
ZHIPU_API_KEY_ADMIN=your_api_key
ZHIPU_MODEL=glm-4

# 服务器配置
PORT=3001
NODE_ENV=production
```

### 模块配置

各模块的配置可以在对应的配置文件中修改：
- `src/config/config.js` - 主配置文件
- 各模块内部配置

## 🔍 故障排查

### 模块未加载

1. 检查日志：`logs/combined.log`
2. 检查模块文件是否存在
3. 检查模块依赖是否正确

### 功能不工作

1. 检查模块是否已初始化
2. 检查API Key配置
3. 查看错误日志

---

**版本**：v3.0.0（智能增强版）
**最后更新**：2025-01-XX






