# 服务器配置
PORT=3000
NODE_ENV=production

# 数据源配置
# RSS源列表（使用分号分隔多个源，格式：名称|URL|类型|地区）
RSS_SOURCES=中国信鸽信息网|https://www.chinaxinge.com/rss|media|national;贵州省信鸽协会|https://example.com/rss|association|local;赛鸽天地|https://example.com/rss|media|national

# API配置
API_KEY=your-api-key-here
API_RATE_LIMIT=100

# 缓存配置
CACHE_TTL_NEWS=3600
CACHE_TTL_EVENTS=300
CACHE_TTL_RESULTS=7200

# 更新频率（秒）
UPDATE_INTERVAL_NEWS=3600
UPDATE_INTERVAL_EVENTS=300
UPDATE_INTERVAL_RESULTS=1800

# 日志配置
LOG_LEVEL=info
LOG_FILE=logs/app.log

# AI配置（支持多个AI服务提供商，优先使用国内可访问的服务）
# 
# 选项1：智谱AI（推荐，国内可访问，免费额度）
# 获取地址：https://open.bigmodel.cn/
# 注册后获取API Key，配置后即可使用，无需VPN
ZHIPU_API_KEY=

# 选项2：通义千问（阿里云，国内可访问）
# 获取地址：https://dashscope.aliyun.com/
# 注册后获取API Key，配置后即可使用，无需VPN
QWEN_API_KEY=

# 选项3：Hugging Face（需要VPN，作为备选）
# 获取地址：https://huggingface.co/settings/tokens
# 有API Key可以获得更高的免费额度
HUGGING_FACE_API_KEY=

# AI模型选择（可选，默认auto自动选择）
# auto: 自动选择可用的AI服务（优先国内服务）
# zhipu: 强制使用智谱AI
# qwen: 强制使用通义千问
# huggingface: 强制使用Hugging Face
AI_MODEL=auto












