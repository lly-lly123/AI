/**
 * 自动账号注册和数据管理脚本
 * 功能：
 * 1. 自动识别设备信息并生成账号
 * 2. 自动保存数据到本地和云端
 * 3. 页面加载时自动调取本地和云端数据
 * 4. 网站更新后自动恢复数据
 */

(function() {
  'use strict';
  
  console.log('🔧 [自动账号] 开始初始化自动账号注册系统...');
  
  // ==================== 1. 设备信息识别 ====================
  
  const DEVICE_ID_KEY = 'pigeon_device_id';
  const DEVICE_INFO_KEY = 'pigeon_device_info';
  const AUTO_ACCOUNT_KEY = 'pigeon_auto_account';
  
  /**
   * 获取或创建设备ID
   */
  function getOrCreateDeviceId() {
    try {
      let deviceId = localStorage.getItem(DEVICE_ID_KEY);
      if (!deviceId) {
        // 生成唯一设备ID
        if (window.crypto && window.crypto.randomUUID) {
          deviceId = window.crypto.randomUUID();
        } else {
          deviceId = 'dev_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
        }
        localStorage.setItem(DEVICE_ID_KEY, deviceId);
        console.log('✅ [自动账号] 已创建新设备ID:', deviceId);
      }
      return deviceId;
    } catch (e) {
      console.warn('⚠️ [自动账号] 生成设备ID失败:', e);
      return null;
    }
  }
  
  /**
   * 获取设备信息
   */
  function getDeviceInfo() {
    try {
      let deviceInfo = localStorage.getItem(DEVICE_INFO_KEY);
      if (deviceInfo) {
        return JSON.parse(deviceInfo);
      }
      
      // 收集设备信息
      deviceInfo = {
        userAgent: navigator.userAgent || '',
        platform: navigator.platform || '',
        language: navigator.language || '',
        screenWidth: window.screen.width || 0,
        screenHeight: window.screen.height || 0,
        deviceType: /Mobile|Android|iPhone|iPad/i.test(navigator.userAgent) ? 'mobile' : 'desktop',
        timestamp: new Date().toISOString()
      };
      
      localStorage.setItem(DEVICE_INFO_KEY, JSON.stringify(deviceInfo));
      return deviceInfo;
    } catch (e) {
      console.warn('⚠️ [自动账号] 获取设备信息失败:', e);
      return {
        userAgent: navigator.userAgent || '',
        platform: navigator.platform || '',
        deviceType: 'unknown'
      };
    }
  }
  
  // ==================== 2. 自动账号注册 ====================
  
  /**
   * 自动注册账号（基于设备ID）
   */
  async function autoRegisterAccount() {
    try {
      // 检查是否已有账号
      const existingAccount = localStorage.getItem(AUTO_ACCOUNT_KEY);
      if (existingAccount) {
        const account = JSON.parse(existingAccount);
        console.log('✅ [自动账号] 使用已有账号:', account.username);
        return account;
      }
      
      const deviceId = getOrCreateDeviceId();
      if (!deviceId) {
        console.warn('⚠️ [自动账号] 无法生成设备ID，跳过自动注册');
        return null;
      }
      
      const deviceInfo = getDeviceInfo();
      const apiUrl = getBackendApiUrl();
      
      // 生成用户名（基于设备ID）
      const username = 'user_' + deviceId.substring(0, 8);
      const email = username + '@auto.local';
      const password = deviceId; // 使用设备ID作为密码
      
      console.log('🔧 [自动账号] 正在自动注册账号...');
      console.log('🔧 [自动账号] API URL:', apiUrl);
      console.log('🔧 [自动账号] 设备信息:', {
        deviceId: deviceId.substring(0, 8) + '...',
        deviceType: deviceInfo.deviceType,
        platform: deviceInfo.platform
      });
      
      // 确保API URL格式正确（如果已经包含/api，不再重复添加）
      let registerUrl = apiUrl;
      if (!registerUrl.endsWith('/api/auth/register')) {
        if (registerUrl.endsWith('/api')) {
          registerUrl = registerUrl + '/auth/register';
        } else if (registerUrl.endsWith('/')) {
          registerUrl = registerUrl + 'api/auth/register';
        } else {
          registerUrl = registerUrl + '/api/auth/register';
        }
      }
      
      console.log('🔧 [自动账号] 注册URL:', registerUrl);
      
      // 尝试注册
      const registerResponse = await fetch(registerUrl, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({
          username: username,
          email: email,
          password: password
        })
      });
      
      let account = null;
      
      if (registerResponse.ok) {
        const result = await registerResponse.json();
        if (result.success) {
          account = {
            username: username,
            email: email,
            deviceId: deviceId,
            deviceInfo: deviceInfo,
            createdAt: new Date().toISOString(),
            autoGenerated: true
          };
          localStorage.setItem(AUTO_ACCOUNT_KEY, JSON.stringify(account));
          console.log('✅ [自动账号] 账号注册成功:', username);
        }
      } else {
        // 如果注册失败（可能用户名已存在），尝试登录
        const errorText = await registerResponse.text();
        console.log('🔧 [自动账号] 注册失败，状态码:', registerResponse.status);
        console.log('🔧 [自动账号] 注册失败，响应:', errorText);
        console.log('🔧 [自动账号] 尝试登录...');
        
        // 确保登录URL格式正确
        let loginUrl = apiUrl;
        if (!loginUrl.endsWith('/api/auth/login')) {
          if (loginUrl.endsWith('/api')) {
            loginUrl = loginUrl + '/auth/login';
          } else if (loginUrl.endsWith('/')) {
            loginUrl = loginUrl + 'api/auth/login';
          } else {
            loginUrl = loginUrl + '/api/auth/login';
          }
        }
        
        console.log('🔧 [自动账号] 登录URL:', loginUrl);
        const loginResponse = await fetch(loginUrl, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({
            username: username,
            password: password
          })
        });
        
        if (loginResponse.ok) {
          const result = await loginResponse.json();
          if (result.success && result.token) {
            account = {
              username: username,
              email: email,
              deviceId: deviceId,
              deviceInfo: deviceInfo,
              createdAt: new Date().toISOString(),
              autoGenerated: true
            };
            localStorage.setItem(AUTO_ACCOUNT_KEY, JSON.stringify(account));
            // 保存token（同时保存到两个键，兼容不同页面）
            const token = result.token;
            localStorage.setItem('auth_token', token);
            localStorage.setItem('token', token);
            if (typeof setAuthToken === 'function') {
              setAuthToken(token);
            }
            
            // 保存用户信息
            if (result.data && result.data.user) {
              localStorage.setItem('userInfo', JSON.stringify(result.data.user));
            } else {
              localStorage.setItem('userInfo', JSON.stringify({
                username: username,
                email: email,
                name: username
              }));
            }
            
            console.log('✅ [自动账号] 登录成功:', username);
            
            // 更新移动端用户按钮显示
            if (typeof window.updateMobileUserButton === 'function') {
              setTimeout(() => {
                window.updateMobileUserButton();
              }, 100);
            }
          }
        }
      }
      
      if (!account) {
        console.warn('⚠️ [自动账号] 未能创建或登录账户，可能的原因：');
        console.warn('  1. 网络连接问题');
        console.warn('  2. 后端服务未启动');
        console.warn('  3. API地址配置错误');
        console.warn('  4. 注册接口返回错误');
      }
      
      return account;
    } catch (error) {
      console.error('❌ [自动账号] 自动注册失败:', error);
      console.error('❌ [自动账号] 错误详情:', {
        message: error.message,
        stack: error.stack,
        apiUrl: apiUrl
      });
      return null;
    }
  }
  
  /**
   * 获取后端API URL
   */
  function getBackendApiUrl() {
    // 尝试从全局变量获取
    if (typeof window !== 'undefined' && window.BACKEND_API_URL) {
      return window.BACKEND_API_URL;
    }
    
    // 尝试从页面的getBackendApiUrl函数获取（如果存在）
    if (typeof window !== 'undefined' && typeof window.getBackendApiUrl === 'function') {
      try {
        const apiUrl = window.getBackendApiUrl();
        if (apiUrl) {
          console.log('✅ [自动账号] 从页面函数获取API URL:', apiUrl);
          return apiUrl;
        }
      } catch (e) {
        console.warn('⚠️ [自动账号] 调用页面getBackendApiUrl失败:', e);
      }
    }
    
    // 从当前页面URL推断
    const hostname = window.location.hostname;
    if (hostname.includes('localhost') || hostname.includes('127.0.0.1')) {
      return 'http://localhost:3000';
    }
    
    // 使用当前域名（自动添加/api路径）
    const origin = window.location.origin;
    const apiUrl = origin + '/api';
    console.log('✅ [自动账号] 使用当前域名作为API URL:', apiUrl);
    return apiUrl;
  }
  
  // ==================== 3. 数据自动保存和调取 ====================
  
  /**
   * 自动保存数据到本地和云端
   */
  async function autoSaveData(dataType, data) {
    try {
      // 1. 保存到本地（使用与页面一致的存储键名）
      let storageKey;
      if (dataType === 'pigeons') {
        storageKey = 'pigeon_manager_data_v1'; // 与PC端和移动端保持一致
      } else if (dataType === 'races') {
        storageKey = 'pigeon_races_v1'; // 与PC端和移动端保持一致
      } else {
        storageKey = `pigeon_${dataType}_v1`;
      }
      localStorage.setItem(storageKey, JSON.stringify(data));
      console.log(`✅ [自动保存] ${dataType}已保存到本地，键名: ${storageKey}`);
      
      // 2. 保存到云端
      const account = JSON.parse(localStorage.getItem(AUTO_ACCOUNT_KEY) || 'null');
      if (!account) {
        // 如果没有账号，先自动注册
        await autoRegisterAccount();
      }
      
      const token = localStorage.getItem('auth_token');
      if (token) {
        const apiUrl = getBackendApiUrl();
        
        try {
          const response = await fetch(apiUrl + '/api/user/data/' + dataType, {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
              'Authorization': 'Bearer ' + token
            },
            body: JSON.stringify({
              [dataType]: data
            })
          });
          
          if (response.ok) {
            console.log(`✅ [自动保存] ${dataType}已保存到云端`);
          } else {
            console.warn(`⚠️ [自动保存] ${dataType}云端保存失败:`, response.status);
          }
        } catch (error) {
          console.warn(`⚠️ [自动保存] ${dataType}云端保存异常:`, error);
        }
      }
    } catch (error) {
      console.error(`❌ [自动保存] ${dataType}保存失败:`, error);
    }
  }
  
  /**
   * 自动调取数据（优先从云端，失败则使用本地）
   */
  async function autoLoadData(dataType) {
    try {
      // 使用与页面一致的存储键名
      let storageKey;
      if (dataType === 'pigeons') {
        storageKey = 'pigeon_manager_data_v1'; // 与PC端和移动端保持一致
      } else if (dataType === 'races') {
        storageKey = 'pigeon_races_v1'; // 与PC端和移动端保持一致
      } else {
        storageKey = `pigeon_${dataType}_v1`;
      }
      let data = null;
      
      // 1. 尝试从云端加载
      const token = localStorage.getItem('auth_token');
      if (token) {
        const apiUrl = getBackendApiUrl();
        
        try {
          const response = await fetch(apiUrl + '/api/user/data/' + dataType, {
            method: 'GET',
            headers: {
              'Authorization': 'Bearer ' + token
            }
          });
          
          if (response.ok) {
            const result = await response.json();
            if (result.success && result.data) {
              data = result.data;
              // 同步到本地
              localStorage.setItem(storageKey, JSON.stringify(data));
              console.log(`✅ [自动调取] ${dataType}已从云端加载并同步到本地`);
              return data;
            }
          }
        } catch (error) {
          console.warn(`⚠️ [自动调取] ${dataType}云端加载失败，使用本地数据:`, error);
        }
      }
      
      // 2. 从本地加载
      const localData = localStorage.getItem(storageKey);
      if (localData) {
        data = JSON.parse(localData);
        console.log(`✅ [自动调取] ${dataType}已从本地加载`);
        return data;
      }
      
      console.log(`ℹ️ [自动调取] ${dataType}暂无数据`);
      return null;
    } catch (error) {
      console.error(`❌ [自动调取] ${dataType}加载失败:`, error);
      return null;
    }
  }
  
  /**
   * 页面加载时自动调取所有数据
   */
  async function autoLoadAllData() {
    console.log('🔧 [自动调取] 开始加载所有数据...');
    
    // 确保有账号
    let account = JSON.parse(localStorage.getItem(AUTO_ACCOUNT_KEY) || 'null');
    if (!account) {
      account = await autoRegisterAccount();
    }
    
    // 如果没有token，尝试登录
    let token = localStorage.getItem('auth_token');
    if (!token && account) {
      const deviceId = getOrCreateDeviceId();
      const apiUrl = getBackendApiUrl();
      
      try {
        const loginResponse = await fetch(apiUrl + '/api/auth/login', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({
            username: account.username,
            password: account.deviceId
          })
        });
        
        if (loginResponse.ok) {
          const result = await loginResponse.json();
          if (result.success && result.token) {
            token = result.token;
            localStorage.setItem('auth_token', token);
            // 同时保存到token键（兼容移动端）
            localStorage.setItem('token', token);
            console.log('✅ [自动调取] 已自动登录');
            
            // 如果返回了用户信息，保存它
            if (result.data && result.data.user) {
              localStorage.setItem('userInfo', JSON.stringify(result.data.user));
              console.log('✅ [自动调取] 已保存用户信息');
            } else if (account) {
              // 如果没有返回用户信息，使用账户信息
              localStorage.setItem('userInfo', JSON.stringify({
                username: account.username,
                email: account.email,
                name: account.username
              }));
            }
            
            // 更新移动端用户按钮显示
            if (typeof window.updateMobileUserButton === 'function') {
              setTimeout(() => {
                window.updateMobileUserButton();
                console.log('✅ [自动调取] 已更新移动端用户按钮');
              }, 100);
            }
          }
        }
      } catch (error) {
        console.warn('⚠️ [自动调取] 自动登录失败:', error);
      }
    }
    
    // 加载所有数据类型
    const dataTypes = ['pigeons', 'races', 'healthRecords', 'trainingRecords', 'pairings', 'qualificationRecords'];
    
    for (const dataType of dataTypes) {
      const data = await autoLoadData(dataType);
      // 如果全局变量存在，更新它们
      if (typeof window !== 'undefined') {
        if (dataType === 'pigeons') {
          if (window.pigeons) {
            // 合并数据，优先使用云端数据
            if (data && Array.isArray(data) && data.length > 0) {
              window.pigeons = data;
            } else if (!window.pigeons || window.pigeons.length === 0) {
              // 如果全局变量为空，尝试从localStorage加载
              const localData = localStorage.getItem('pigeon_manager_data_v1');
              if (localData) {
                try {
                  window.pigeons = JSON.parse(localData);
                  console.log('✅ [自动调取] 从localStorage恢复pigeons数据');
                } catch (e) {
                  console.warn('⚠️ [自动调取] 解析localStorage数据失败:', e);
                }
              }
            }
          } else {
            // 如果全局变量不存在，创建它
            window.pigeons = data || [];
          }
        } else if (dataType === 'races') {
          if (window.races) {
            if (data && Array.isArray(data) && data.length > 0) {
              window.races = data;
            } else if (!window.races || window.races.length === 0) {
              const localData = localStorage.getItem('pigeon_races_v1');
              if (localData) {
                try {
                  window.races = JSON.parse(localData);
                  console.log('✅ [自动调取] 从localStorage恢复races数据');
                } catch (e) {
                  console.warn('⚠️ [自动调取] 解析localStorage数据失败:', e);
                }
              }
            }
          } else {
            window.races = data || [];
          }
        }
        // 其他数据类型类似处理
      }
    }
    
    // 触发页面刷新（如果页面有刷新函数）
    if (typeof window !== 'undefined') {
      // 触发自定义事件，让页面知道数据已加载
      window.dispatchEvent(new CustomEvent('pigeonDataLoaded', {
        detail: { 
          pigeons: window.pigeons || [],
          races: window.races || []
        }
      }));
      
      // 如果页面有刷新函数，调用它们
      if (typeof window.refreshList === 'function') {
        setTimeout(() => window.refreshList(), 100);
      }
      if (typeof window.refreshDashboard === 'function') {
        setTimeout(() => window.refreshDashboard(), 100);
      }
      if (typeof window.loadData === 'function') {
        setTimeout(() => window.loadData(), 100);
      }
    }
    
    console.log('✅ [自动调取] 所有数据加载完成');
    
    // 触发数据加载完成事件
    if (typeof window !== 'undefined') {
      window.dispatchEvent(new CustomEvent('dataAutoLoaded', {
        detail: { account, token }
      }));
    }
  }
  
  // ==================== 4. 初始化 ====================
  
  /**
   * 初始化自动账号系统
   */
  async function init() {
    console.log('🔧 [自动账号] 初始化自动账号系统...');
    
    // 等待页面加载完成
    const initFunction = async () => {
      // 1. 自动注册账号
      await autoRegisterAccount();
      
      // 2. 自动加载所有数据
      await autoLoadAllData();
      
      // 3. 如果页面有loadFromStorage函数，也调用它（兼容现有代码）
      if (typeof window.loadFromStorage === 'function') {
        try {
          await window.loadFromStorage();
          console.log('✅ [自动账号] 已调用页面loadFromStorage函数');
        } catch (error) {
          console.warn('⚠️ [自动账号] 调用loadFromStorage失败:', error);
        }
      }
      
      // 4. 如果页面有loadData函数，也调用它（移动端）
      if (typeof window.loadData === 'function') {
        try {
          window.loadData();
          console.log('✅ [自动账号] 已调用页面loadData函数');
        } catch (error) {
          console.warn('⚠️ [自动账号] 调用loadData失败:', error);
        }
      }
      
      // 5. 如果页面有restoreUserDataIfMissing函数，也调用它
      if (typeof window.restoreUserDataIfMissing === 'function') {
        try {
          await window.restoreUserDataIfMissing();
          console.log('✅ [自动账号] 已调用页面restoreUserDataIfMissing函数');
        } catch (error) {
          console.warn('⚠️ [自动账号] 调用restoreUserDataIfMissing失败:', error);
        }
      }
      
      // 6. 如果已自动登录，隐藏登录弹窗
      const token = localStorage.getItem('auth_token') || localStorage.getItem('token');
      if (token && typeof window.closeLoginModal === 'function') {
        setTimeout(() => {
          window.closeLoginModal();
          console.log('✅ [自动账号] 已自动登录，隐藏登录弹窗');
        }, 500);
      }
      
      // 7. 触发数据加载完成事件，让页面刷新视图
      if (typeof window !== 'undefined') {
        const account = JSON.parse(localStorage.getItem(AUTO_ACCOUNT_KEY) || 'null');
        const finalToken = localStorage.getItem('auth_token') || localStorage.getItem('token');
        console.log('🔔 [自动账号] 触发dataAutoLoaded事件，account:', account ? account.username : 'null', 'token:', finalToken ? '已存在' : '不存在');
        window.dispatchEvent(new CustomEvent('dataAutoLoaded', {
          detail: { 
            account: account,
            token: finalToken
          }
        }));
      }
    };
    
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initFunction);
    } else {
      // 延迟一点执行，确保其他脚本已加载
      setTimeout(initFunction, 100);
    }
    
    // 监听数据变化，自动保存
    if (typeof window !== 'undefined') {
      // 监听自定义事件
      window.addEventListener('dataChanged', async (event) => {
        const { dataType, data } = event.detail;
        if (dataType && data) {
          await autoSaveData(dataType, data);
        }
      });
      
      // 定期自动保存（每5分钟）
      setInterval(async () => {
        if (window.pigeons) {
          await autoSaveData('pigeons', window.pigeons);
        }
        if (window.races) {
          await autoSaveData('races', window.races);
        }
      }, 5 * 60 * 1000);
    }
    
    console.log('✅ [自动账号] 自动账号系统初始化完成');
  }
  
  // 暴露到全局
  if (typeof window !== 'undefined') {
    window.autoRegisterAccount = autoRegisterAccount;
    window.autoSaveData = autoSaveData;
    window.autoLoadData = autoLoadData;
    window.autoLoadAllData = autoLoadAllData;
    window.getOrCreateDeviceId = getOrCreateDeviceId;
    window.getDeviceInfo = getDeviceInfo;
  }
  
  // 立即初始化
  init();
  
})();

