# 中枢管家智能升级快速指南

> 快速了解和使用升级后的智能功能

## 🚀 快速开始

### 1. 启动服务

```bash
cd backend/core-admin
npm start
```

### 2. 验证升级

访问 `http://localhost:3001/api/status`，查看 `intelligent` 字段，确认所有智能模块已加载。

## 🎯 核心智能功能

### 1. 智能决策引擎

**功能**：自动决策系统问题

**使用示例**：
```bash
# 获取系统健康决策建议
curl http://localhost:3001/api/intelligent/advice?category=systemHealth

# 获取API健康决策建议
curl http://localhost:3001/api/intelligent/advice?category=apiHealth
```

**自动执行**：
- 低风险决策自动执行
- 高风险决策需要确认
- 所有决策记录历史

### 2. 预测性维护

**功能**：预测潜在问题

**自动执行**：
- 每30分钟自动检查
- 预测API故障、数据质量、性能问题
- 自动创建告警

**查看预测**：
```bash
curl http://localhost:3001/api/intelligent/predictions
```

### 3. 自动化工作流

**内置工作流**：
- ✅ 每日健康检查（每天9点）
- ✅ 数据清理（每天3点）
- ✅ 性能优化（每6小时）
- ✅ 安全扫描（每12小时）
- ✅ 智能备份（每天2点）

**手动执行**：
```bash
# 查看所有工作流
curl http://localhost:3001/api/intelligent/workflows

# 执行工作流
curl -X POST http://localhost:3001/api/intelligent/workflows/{workflowId}/execute
```

### 4. 智能告警

**告警级别**：
- 🔴 Critical：立即通知，自动干预
- 🟠 High：通知，增强监控
- 🟡 Medium：记录，安排检查
- 🟢 Low：仅记录

**查看告警**：
```bash
# 查看所有告警
curl http://localhost:3001/api/intelligent/alerts

# 查看活跃告警
curl http://localhost:3001/api/intelligent/alerts?status=active

# 查看关键告警
curl http://localhost:3001/api/intelligent/alerts?severity=critical
```

### 5. 自适应优化

**功能**：自动优化系统配置

**自动执行**：
- 每小时自动分析性能
- 自动调整缓存TTL
- 自动优化API重试策略
- 自动调整并发控制

**查看优化状态**：
```bash
curl http://localhost:3001/api/intelligent/optimization
```

### 6. 知识图谱

**功能**：系统知识库

**查询示例**：
```bash
# 查询API相关问题
curl "http://localhost:3001/api/intelligent/knowledge?query=API故障"

# 查询数据质量问题
curl "http://localhost:3001/api/intelligent/knowledge?query=数据质量"
```

### 7. 智能报告

**报告类型**：
- 每日报告（每天1点自动生成）
- 每周报告（每周一2点自动生成）
- 每月报告（每月1号3点自动生成）

**生成报告**：
```bash
# 生成每日报告
curl -X POST http://localhost:3001/api/intelligent/reports/generate \
  -H "Content-Type: application/json" \
  -d '{"type": "daily"}'

# 查看最新报告
curl http://localhost:3001/api/intelligent/reports?type=daily
```

## 📊 监控面板

### 系统状态总览

访问 `http://localhost:3001/api/status` 查看：
- 核心功能状态
- 增强功能状态
- **智能功能状态**（新增）

### 智能功能统计

```json
{
  "intelligent": {
    "decisionEngine": {
      "total": 100,
      "executed": 80,
      "successRate": 0.85
    },
    "predictiveMaintenance": {
      "total": 50,
      "active": 5,
      "accuracy": {
        "apiFailure": 0.75,
        "dataQuality": 0.80
      }
    },
    "workflowEngine": {
      "workflows": 5,
      "running": 0,
      "successRate": 0.90
    },
    "alertSystem": {
      "activeAlerts": 3,
      "channels": ["log"]
    },
    "adaptiveOptimizer": {
      "optimizationsApplied": 20,
      "successRate": 0.80
    },
    "knowledgeGraph": {
      "nodes": 500,
      "edges": 1000
    },
    "intelligentReporting": {
      "reportsGenerated": 30,
      "aiEnhanced": true
    }
  }
}
```

## 🎨 后台界面使用

### 1. 查看智能功能状态

登录后台 → 中枢管家 → 系统状态

### 2. 查看智能建议

登录后台 → 中枢管家 → 智能建议

### 3. 管理工作流

登录后台 → 中枢管家 → 工作流管理

### 4. 查看告警

登录后台 → 中枢管家 → 告警中心

### 5. 查看报告

登录后台 → 中枢管家 → 智能报告

## ⚙️ 配置说明

### 环境变量

确保 `.env` 文件中配置了AI API Key：

```env
# 中枢管家使用的API Key
ZHIPU_API_KEY_ADMIN=your_api_key
ZHIPU_MODEL=glm-4
```

### 智能功能开关

所有智能功能默认启用，如需调整：

1. **决策引擎**：修改 `decision-engine.js` 中的 `confidenceThreshold`
2. **预测性维护**：修改 `predictive-maintenance.js` 中的阈值
3. **工作流**：在 `workflow-engine.js` 中注册/取消注册工作流
4. **告警**：修改 `alert-system.js` 中的告警规则

## 🔍 故障排查

### 智能功能未启动

1. 检查日志：`logs/combined.log`
2. 检查AI API Key配置
3. 检查模块依赖是否安装

### AI调用失败

1. 检查网络连接
2. 检查API Key有效性
3. 查看 `logs/error.log`

### 工作流未执行

1. 检查Cron表达式是否正确
2. 检查工作流是否已注册
3. 查看工作流执行日志

## 📚 相关文档

- [中枢管家智能升级完成说明.md](./中枢管家智能升级完成说明.md) - 详细升级说明
- [系统说明.md](./backend/core-admin/系统说明.md) - 系统架构说明
- [超级管理员功能说明.md](./超级管理员功能说明.md) - 功能说明

---

**版本**：v3.0.0（智能增强版）
**最后更新**：2025-01-XX







> 快速了解和使用升级后的智能功能

## 🚀 快速开始

### 1. 启动服务

```bash
cd backend/core-admin
npm start
```

### 2. 验证升级

访问 `http://localhost:3001/api/status`，查看 `intelligent` 字段，确认所有智能模块已加载。

## 🎯 核心智能功能

### 1. 智能决策引擎

**功能**：自动决策系统问题

**使用示例**：
```bash
# 获取系统健康决策建议
curl http://localhost:3001/api/intelligent/advice?category=systemHealth

# 获取API健康决策建议
curl http://localhost:3001/api/intelligent/advice?category=apiHealth
```

**自动执行**：
- 低风险决策自动执行
- 高风险决策需要确认
- 所有决策记录历史

### 2. 预测性维护

**功能**：预测潜在问题

**自动执行**：
- 每30分钟自动检查
- 预测API故障、数据质量、性能问题
- 自动创建告警

**查看预测**：
```bash
curl http://localhost:3001/api/intelligent/predictions
```

### 3. 自动化工作流

**内置工作流**：
- ✅ 每日健康检查（每天9点）
- ✅ 数据清理（每天3点）
- ✅ 性能优化（每6小时）
- ✅ 安全扫描（每12小时）
- ✅ 智能备份（每天2点）

**手动执行**：
```bash
# 查看所有工作流
curl http://localhost:3001/api/intelligent/workflows

# 执行工作流
curl -X POST http://localhost:3001/api/intelligent/workflows/{workflowId}/execute
```

### 4. 智能告警

**告警级别**：
- 🔴 Critical：立即通知，自动干预
- 🟠 High：通知，增强监控
- 🟡 Medium：记录，安排检查
- 🟢 Low：仅记录

**查看告警**：
```bash
# 查看所有告警
curl http://localhost:3001/api/intelligent/alerts

# 查看活跃告警
curl http://localhost:3001/api/intelligent/alerts?status=active

# 查看关键告警
curl http://localhost:3001/api/intelligent/alerts?severity=critical
```

### 5. 自适应优化

**功能**：自动优化系统配置

**自动执行**：
- 每小时自动分析性能
- 自动调整缓存TTL
- 自动优化API重试策略
- 自动调整并发控制

**查看优化状态**：
```bash
curl http://localhost:3001/api/intelligent/optimization
```

### 6. 知识图谱

**功能**：系统知识库

**查询示例**：
```bash
# 查询API相关问题
curl "http://localhost:3001/api/intelligent/knowledge?query=API故障"

# 查询数据质量问题
curl "http://localhost:3001/api/intelligent/knowledge?query=数据质量"
```

### 7. 智能报告

**报告类型**：
- 每日报告（每天1点自动生成）
- 每周报告（每周一2点自动生成）
- 每月报告（每月1号3点自动生成）

**生成报告**：
```bash
# 生成每日报告
curl -X POST http://localhost:3001/api/intelligent/reports/generate \
  -H "Content-Type: application/json" \
  -d '{"type": "daily"}'

# 查看最新报告
curl http://localhost:3001/api/intelligent/reports?type=daily
```

## 📊 监控面板

### 系统状态总览

访问 `http://localhost:3001/api/status` 查看：
- 核心功能状态
- 增强功能状态
- **智能功能状态**（新增）

### 智能功能统计

```json
{
  "intelligent": {
    "decisionEngine": {
      "total": 100,
      "executed": 80,
      "successRate": 0.85
    },
    "predictiveMaintenance": {
      "total": 50,
      "active": 5,
      "accuracy": {
        "apiFailure": 0.75,
        "dataQuality": 0.80
      }
    },
    "workflowEngine": {
      "workflows": 5,
      "running": 0,
      "successRate": 0.90
    },
    "alertSystem": {
      "activeAlerts": 3,
      "channels": ["log"]
    },
    "adaptiveOptimizer": {
      "optimizationsApplied": 20,
      "successRate": 0.80
    },
    "knowledgeGraph": {
      "nodes": 500,
      "edges": 1000
    },
    "intelligentReporting": {
      "reportsGenerated": 30,
      "aiEnhanced": true
    }
  }
}
```

## 🎨 后台界面使用

### 1. 查看智能功能状态

登录后台 → 中枢管家 → 系统状态

### 2. 查看智能建议

登录后台 → 中枢管家 → 智能建议

### 3. 管理工作流

登录后台 → 中枢管家 → 工作流管理

### 4. 查看告警

登录后台 → 中枢管家 → 告警中心

### 5. 查看报告

登录后台 → 中枢管家 → 智能报告

## ⚙️ 配置说明

### 环境变量

确保 `.env` 文件中配置了AI API Key：

```env
# 中枢管家使用的API Key
ZHIPU_API_KEY_ADMIN=your_api_key
ZHIPU_MODEL=glm-4
```

### 智能功能开关

所有智能功能默认启用，如需调整：

1. **决策引擎**：修改 `decision-engine.js` 中的 `confidenceThreshold`
2. **预测性维护**：修改 `predictive-maintenance.js` 中的阈值
3. **工作流**：在 `workflow-engine.js` 中注册/取消注册工作流
4. **告警**：修改 `alert-system.js` 中的告警规则

## 🔍 故障排查

### 智能功能未启动

1. 检查日志：`logs/combined.log`
2. 检查AI API Key配置
3. 检查模块依赖是否安装

### AI调用失败

1. 检查网络连接
2. 检查API Key有效性
3. 查看 `logs/error.log`

### 工作流未执行

1. 检查Cron表达式是否正确
2. 检查工作流是否已注册
3. 查看工作流执行日志

## 📚 相关文档

- [中枢管家智能升级完成说明.md](./中枢管家智能升级完成说明.md) - 详细升级说明
- [系统说明.md](./backend/core-admin/系统说明.md) - 系统架构说明
- [超级管理员功能说明.md](./超级管理员功能说明.md) - 功能说明

---

**版本**：v3.0.0（智能增强版）
**最后更新**：2025-01-XX







> 快速了解和使用升级后的智能功能

## 🚀 快速开始

### 1. 启动服务

```bash
cd backend/core-admin
npm start
```

### 2. 验证升级

访问 `http://localhost:3001/api/status`，查看 `intelligent` 字段，确认所有智能模块已加载。

## 🎯 核心智能功能

### 1. 智能决策引擎

**功能**：自动决策系统问题

**使用示例**：
```bash
# 获取系统健康决策建议
curl http://localhost:3001/api/intelligent/advice?category=systemHealth

# 获取API健康决策建议
curl http://localhost:3001/api/intelligent/advice?category=apiHealth
```

**自动执行**：
- 低风险决策自动执行
- 高风险决策需要确认
- 所有决策记录历史

### 2. 预测性维护

**功能**：预测潜在问题

**自动执行**：
- 每30分钟自动检查
- 预测API故障、数据质量、性能问题
- 自动创建告警

**查看预测**：
```bash
curl http://localhost:3001/api/intelligent/predictions
```

### 3. 自动化工作流

**内置工作流**：
- ✅ 每日健康检查（每天9点）
- ✅ 数据清理（每天3点）
- ✅ 性能优化（每6小时）
- ✅ 安全扫描（每12小时）
- ✅ 智能备份（每天2点）

**手动执行**：
```bash
# 查看所有工作流
curl http://localhost:3001/api/intelligent/workflows

# 执行工作流
curl -X POST http://localhost:3001/api/intelligent/workflows/{workflowId}/execute
```

### 4. 智能告警

**告警级别**：
- 🔴 Critical：立即通知，自动干预
- 🟠 High：通知，增强监控
- 🟡 Medium：记录，安排检查
- 🟢 Low：仅记录

**查看告警**：
```bash
# 查看所有告警
curl http://localhost:3001/api/intelligent/alerts

# 查看活跃告警
curl http://localhost:3001/api/intelligent/alerts?status=active

# 查看关键告警
curl http://localhost:3001/api/intelligent/alerts?severity=critical
```

### 5. 自适应优化

**功能**：自动优化系统配置

**自动执行**：
- 每小时自动分析性能
- 自动调整缓存TTL
- 自动优化API重试策略
- 自动调整并发控制

**查看优化状态**：
```bash
curl http://localhost:3001/api/intelligent/optimization
```

### 6. 知识图谱

**功能**：系统知识库

**查询示例**：
```bash
# 查询API相关问题
curl "http://localhost:3001/api/intelligent/knowledge?query=API故障"

# 查询数据质量问题
curl "http://localhost:3001/api/intelligent/knowledge?query=数据质量"
```

### 7. 智能报告

**报告类型**：
- 每日报告（每天1点自动生成）
- 每周报告（每周一2点自动生成）
- 每月报告（每月1号3点自动生成）

**生成报告**：
```bash
# 生成每日报告
curl -X POST http://localhost:3001/api/intelligent/reports/generate \
  -H "Content-Type: application/json" \
  -d '{"type": "daily"}'

# 查看最新报告
curl http://localhost:3001/api/intelligent/reports?type=daily
```

## 📊 监控面板

### 系统状态总览

访问 `http://localhost:3001/api/status` 查看：
- 核心功能状态
- 增强功能状态
- **智能功能状态**（新增）

### 智能功能统计

```json
{
  "intelligent": {
    "decisionEngine": {
      "total": 100,
      "executed": 80,
      "successRate": 0.85
    },
    "predictiveMaintenance": {
      "total": 50,
      "active": 5,
      "accuracy": {
        "apiFailure": 0.75,
        "dataQuality": 0.80
      }
    },
    "workflowEngine": {
      "workflows": 5,
      "running": 0,
      "successRate": 0.90
    },
    "alertSystem": {
      "activeAlerts": 3,
      "channels": ["log"]
    },
    "adaptiveOptimizer": {
      "optimizationsApplied": 20,
      "successRate": 0.80
    },
    "knowledgeGraph": {
      "nodes": 500,
      "edges": 1000
    },
    "intelligentReporting": {
      "reportsGenerated": 30,
      "aiEnhanced": true
    }
  }
}
```

## 🎨 后台界面使用

### 1. 查看智能功能状态

登录后台 → 中枢管家 → 系统状态

### 2. 查看智能建议

登录后台 → 中枢管家 → 智能建议

### 3. 管理工作流

登录后台 → 中枢管家 → 工作流管理

### 4. 查看告警

登录后台 → 中枢管家 → 告警中心

### 5. 查看报告

登录后台 → 中枢管家 → 智能报告

## ⚙️ 配置说明

### 环境变量

确保 `.env` 文件中配置了AI API Key：

```env
# 中枢管家使用的API Key
ZHIPU_API_KEY_ADMIN=your_api_key
ZHIPU_MODEL=glm-4
```

### 智能功能开关

所有智能功能默认启用，如需调整：

1. **决策引擎**：修改 `decision-engine.js` 中的 `confidenceThreshold`
2. **预测性维护**：修改 `predictive-maintenance.js` 中的阈值
3. **工作流**：在 `workflow-engine.js` 中注册/取消注册工作流
4. **告警**：修改 `alert-system.js` 中的告警规则

## 🔍 故障排查

### 智能功能未启动

1. 检查日志：`logs/combined.log`
2. 检查AI API Key配置
3. 检查模块依赖是否安装

### AI调用失败

1. 检查网络连接
2. 检查API Key有效性
3. 查看 `logs/error.log`

### 工作流未执行

1. 检查Cron表达式是否正确
2. 检查工作流是否已注册
3. 查看工作流执行日志

## 📚 相关文档

- [中枢管家智能升级完成说明.md](./中枢管家智能升级完成说明.md) - 详细升级说明
- [系统说明.md](./backend/core-admin/系统说明.md) - 系统架构说明
- [超级管理员功能说明.md](./超级管理员功能说明.md) - 功能说明

---

**版本**：v3.0.0（智能增强版）
**最后更新**：2025-01-XX






